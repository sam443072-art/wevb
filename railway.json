{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python -c \"import flet as ft; import os; from main import main; port = int(os.environ.get('PORT', 8080)); ft.app(target=main, view=ft.AppView.WEB_BROWSER, port=port, host='0.0.0.0')\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
